/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
	images: {
		domains: ['lh3.googleusercontent.com', 'ikzttp.mypinata.cloud', 'ipfs.io'],
	}
};

module.exports = nextConfig;
